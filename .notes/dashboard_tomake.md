# Dashboard V2 — Requirements & Implementation

## 🟤 Guest (ไม่ต้อง Login)

### Dashboard (`/dashboard`)

- จำนวนรายงานทั้งหมดที่ been reported
- แผนที่การรายงาน (ย้อนหลัง 30 วัน)
- Top 5 ศัตรูพืช เรียงตามความรุนแรง (30 วัน)

---

## 🟢 User (Login แล้ว)

### Dashboard (`/dashboard`)

- **Global Tab**: เหมือน Guest ทั้งหมด (ข้อมูลภาพรวม global)
- **My Reports Tab**: ดูรายงานที่ตัวเองบันทึก เพื่อติดตามสถานการณ์ในแปลงของตัวเอง
  - Stats: รายงานทั้งหมด / อนุมัติ / รอตรวจ / ไม่อนุมัติ
  - ตารางรายงานล่าสุด 10 รายการ พร้อมสถานะ
  - ถ้า REJECTED → แสดงเหตุผลที่ถูก reject
  - ปุ่ม Export CSV

### User Menu

- **Dashboard** → `/dashboard`
- **My Reports** → `/my-reports`
- **Profile** → `/profile`

---

## 🔵 Expert (Login แล้ว, role = EXPERT)

### Dashboard (`/dashboard`)

- **เหมือน User ทั้งหมด** + Expert badge
- **Review Reports button** → `/review`
- Analytics section — TBD (ข้ามไว้ก่อน)

### User Menu

- เหมือน User + **Expert Review** → `/review`

### Review Page (`/review`) — TBD

- เห็นรายงานจาก User ทั้งหมด
- อ่านรายงาน → ตรวจสอบความถูกต้อง
- กด APPROVE หรือ REJECT + ใส่ note/เหตุผล
- สถานะจะเปลี่ยนกลับไปที่ User เห็น

---

## Route Structure

```
/dashboard          ← Unified dashboard (Guest/User/Expert ตาม role)
/dashboard/admin    ← Admin only (ไม่ได้แก้ไข)
/review             ← Expert only (ตรวจสอบรายงาน) — TBD
/my-reports         ← User/Expert (รายงานของตัวเอง) — มีอยู่แล้ว
/profile            ← User/Expert (แก้ไข profile) — มีอยู่แล้ว
```

## Implementation Status

- [x] Middleware: `/dashboard` ไม่ redirect ตาม role อีกต่อไป (ADMIN → `/dashboard/admin` เท่านั้น)
- [x] Login redirect: ทุก role ไปที่ `/dashboard` (ยกเว้น ADMIN)
- [x] Navbar / UserMenu: ลิงก์ชี้ไปที่ `/dashboard` เดียว
- [x] `DashboardClient.tsx`: Unified — Guest, User, Expert เห็นตาม role
- [x] `actions.ts`: รวม data fetching ทั้งหมดไว้ในไฟล์เดียว
- [x] Old folders: `/dashboard/user` & `/dashboard/expert` redirect ไปที่ `/dashboard`
- [x] TypeScript compile: ผ่าน ไม่มี error
- [x] `/review` page — Expert Review สร้างเสร็จแล้ว (actions + client)
