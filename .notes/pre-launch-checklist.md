# 🚀 Pre-Launch Checklist — TRPMN (RicePestNet)

> **สำหรับ:** ตรวจสอบทุกอย่างก่อน จดทะเบียนโดเมน, เลือก Hosting, และเปิดใช้งานจริง
> **Branch:** `polish110269`
> **อัปเดตล่าสุด:** 11 กุมภาพันธ์ 2569

---

## ✅ Phase 1: ตรวจสอบ Code & Build

- [ ] **1.1 Build Production สำเร็จ** — รัน `npm run build` ต้องไม่มี Error
- [ ] **1.2 ESLint ผ่าน** — รัน `npm run lint` ต้องไม่มี Error
- [ ] **1.3 TypeScript ไม่มี Error** — `ignoreBuildErrors: false` ยืนยันแล้วใน `next.config.ts` ✅
- [ ] **1.4 ลบ Console.log ที่ไม่จำเป็น** — `removeConsole` ถูกตั้งค่าใน production ✅
- [ ] **1.5 ลบไฟล์ทดสอบที่ root** — มีไฟล์ `test-*.mjs` หลายไฟล์ที่ root ให้ลบหรือย้ายออก:
  - `test-admin-db-logic.mjs`
  - `test-dashboard-metrics.mjs`
  - `test-expert-access.mjs`
  - `test-expert-user-dash.mjs`
  - `test-export.mjs`
  - `test-master-data.mjs`
  - `test-public-dash.mjs`
  - `check-dates.ts`
  - `check_roles.js`
- [ ] **1.6 ลบไฟล์ default ที่ไม่ใช้** — ใน `/public`:
  - `file.svg`, `globe.svg`, `next.svg`, `vercel.svg`, `window.svg` (ถ้าไม่ได้ใช้)

---

## 🌐 Phase 2: SEO & Metadata

- [ ] **2.1 ตั้ง `lang="th"`** — ใน `layout.tsx` ตอนนี้เป็น `lang="en"` ❌ ต้องเปลี่ยนเป็น `"th"`
- [ ] **2.2 Update `NEXT_PUBLIC_APP_URL`** — ใน `.env` ต้องใส่ URL จริง (เช่น `https://ricepestnet.go.th`)
- [ ] **2.3 Open Graph / Social Media Meta Tags** — ตรวจสอบ title, description, image
- [ ] **2.4 Favicon & Apple Touch Icon** — มี `favicon.ico` และ `logo.png` อยู่แล้ว ✅
- [ ] **2.5 เพิ่ม `robots.txt`** — สร้างไฟล์ `/public/robots.txt`
- [ ] **2.6 เพิ่ม `sitemap.xml`** — สร้าง sitemap สำหรับ search engines
- [ ] **2.7 Title ทุกหน้าเป็นภาษาไทย** — ตรวจสอบ metadata ของทุกหน้า

---

## 🔒 Phase 3: Security & Environment

- [ ] **3.1 ตั้งค่า `ALLOWED_ORIGIN`** — ในไฟล์ `.env` / `next.config.ts` ตอนนี้เป็น `"*"` ❌ ต้องใส่โดเมนจริง
- [ ] **3.2 ตรวจสอบ `.env` ไม่ขึ้น Git** — ตรวจว่า `.gitignore` มี `.env` แล้ว
- [ ] **3.3 ตั้ง Environment Variables บน Hosting** — ย้ายค่าจาก `.env` ไปตั้งบน hosting provider:
  - `NEXT_PUBLIC_SUPABASE_URL`
  - `NEXT_PUBLIC_SUPABASE_ANON_KEY`
  - `DATABASE_URL` (Prisma/Supabase)
  - `NEXT_PUBLIC_APP_URL`
  - `ALLOWED_ORIGIN`
- [ ] **3.4 Security Headers** — มีตั้งค่าแล้วใน `next.config.ts` ✅
- [ ] **3.5 RLS (Row Level Security)** — ตรวจสอบว่า Supabase tables ทุกตารางเปิด RLS
- [ ] **3.6 API Rate Limiting** — พิจารณาเพิ่ม rate limiting สำหรับ API routes

---

## 📄 Phase 4: หน้ากฎหมาย (Legal Pages)

- [ ] **4.1 Privacy Policy** — มีแล้ว ✅ แต่ต้องตรวจสอบ:
  - เบอร์โทร `0-XXXX-XXXX` → ❌ ต้องใส่เบอร์จริง
  - อีเมล `contact@ricepestnet.go.th` → ❌ ต้องเปลี่ยนเป็นอีเมลจริงหรือสร้างอีเมลนี้
  - โดเมน `ricepestmonitor.net.th` ที่อ้างอิง → ต้องตรงกับโดเมนจริงที่จะจด
- [ ] **4.2 Terms of Use** — มีแล้ว ✅
- [ ] **4.3 Disclaimer** — มีแล้ว ✅
- [ ] **4.4 Cookie Consent Banner** — ตามที่ Privacy Policy ระบุว่ามี Cookie ควรเพิ่ม Cookie consent banner

---

## 🎨 Phase 5: UI/UX ตรวจสอบทุกหน้า

### หน้าหลัก & นำทาง
- [ ] **5.1 Landing Page** — ตรวจสอบ layout, hero, CTA ทั้ง light/dark mode
- [ ] **5.2 Navbar** — responsive mobile + desktop ถูกต้อง
- [ ] **5.3 Footer** — ลิงก์ถูกต้อง, ข้อมูลติดต่อถูกต้อง

### หน้าเนื้อหา
- [ ] **5.4 About Page** — ข้อมูลผู้พัฒนาและรูปภาพ `ME.jpg` แสดงถูกต้อง
- [ ] **5.5 Survey Form** — ทดสอบกรอกข้อมูลครบทุก step
- [ ] **5.6 Dashboard (Public)** — แสดงข้อมูลได้ถูกต้อง
- [ ] **5.7 Dashboard (User)** — ข้อมูลส่วนตัวแสดงถูกต้องหลัง login
- [ ] **5.8 Dashboard (Expert)** — สิทธิ์เข้าถึงถูกต้อง
- [ ] **5.9 Dashboard (Admin)** — การจัดการ user + reports ทำงานได้
- [ ] **5.10 Login / Signup** — ทดสอบ flow สมัคร + เข้าสู่ระบบ
- [ ] **5.11 Profile** — แก้ไขข้อมูลส่วนตัวได้
- [ ] **5.12 Reports Detail** — หน้ารายละเอียดรายงาน `/reports/[id]`
- [ ] **5.13 Expert Review** — flow การ verify reports

### Legal Pages
- [ ] **5.14 Privacy, Terms, Disclaimer** — เนื้อหาครบถ้วน, ลิงก์กลับหน้าหลักทำงาน

---

## 📱 Phase 6: Responsive & Cross-Browser

- [ ] **6.1 Mobile (< 640px)** — ทดสอบทุกหน้าบน mobile
- [ ] **6.2 Tablet (768px - 1024px)** — ทดสอบ layout
- [ ] **6.3 Desktop (> 1024px)** — ปกติ
- [ ] **6.4 Dark Mode / Light Mode** — ทดสอบ toggle theme ทุกหน้า
- [ ] **6.5 Chrome, Safari, Firefox** — ทดสอบ cross-browser

---

## ⚡ Phase 7: Performance

- [ ] **7.1 Image Optimization** — ภาพใน `/public` ไฟล์ใหญ่:
  - `rice_plant.jpg` (9.9 MB!) → ❌ ต้องลดขนาดให้ < 500 KB
  - `cover.jpg` (3.6 MB) → ❌ ต้องลดขนาดให้ < 500 KB
  - `logo.png` (1.2 MB) → ❌ ต้องลดขนาดให้ < 200 KB
  - `about1.jpg` (831 KB) → ⚠️ ควรลดขนาด
- [ ] **7.2 Lighthouse Score** — ตรวจสอบ Performance, Accessibility, SEO, Best Practices
- [ ] **7.3 Web Vitals** — LCP, FID, CLS ต้องอยู่ในเกณฑ์ดี
- [ ] **7.4 Font Loading** — Noto Sans Thai ใช้ `display: swap` ✅

---

## 🗄️ Phase 8: Database & Backend

- [ ] **8.1 Prisma migrate** — migration ล่าสุดพร้อม deploy
- [ ] **8.2 Seed data** — ข้อมูลพื้นฐาน (provinces, plants, pests) ครบถ้วน
- [ ] **8.3 Supabase project** — ต้องใช้ production project (ไม่ใช่ dev)
- [ ] **8.4 Database backup** — ตั้ง backup schedule
- [ ] **8.5 Supabase Auth** — ตั้งค่า redirect URLs ให้ตรงกับโดเมนจริง
- [ ] **8.6 Email Template** — Supabase confirmation/reset email ปรับแต่งแล้ว

---

## 🌍 Phase 9: Domain & Hosting

### Domain (โดเมน)
- [ ] **9.1 เลือกชื่อโดเมน** — เช่น:
  - `ricepestnet.go.th` (ภาครัฐ — ต้องขอผ่าน THNIC)
  - `ricepestnet.or.th` (องค์กร)
  - `ricepestnet.in.th` (ส่วนบุคคล/ทั่วไป)
  - `trpmn.or.th`
- [ ] **9.2 จดทะเบียนโดเมน** — ผ่าน Thai NIC (thnic.or.th) หรือ registrar อื่น
- [ ] **9.3 ตั้ง DNS** — ชี้ไปยัง hosting

### Hosting (แนะนำ)
- [ ] **9.4 เลือก Hosting Platform** — ตัวเลือก:
  - **Vercel** (แนะนำสำหรับ Next.js) — ใช้งานง่าย, free tier ดี, auto deploy จาก GitHub
  - **Netlify** — ทางเลือก, ต้องใช้ adapter สำหรับ Next.js 16
  - **AWS / GCP / Azure** — ถ้าต้องการ control เต็มที่
  - **Railway / Render** — ราคาประหยัด
- [ ] **9.5 ตั้ง Custom Domain บน Hosting**
- [ ] **9.6 ตั้ง SSL Certificate** — HTTPS (ส่วนใหญ่ hosting จัดให้ฟรี)
- [ ] **9.7 ตั้ง CI/CD** — auto deploy จาก `main` branch

---

## 📧 Phase 10: สื่อสาร & Monitoring

- [ ] **10.1 ตั้งค่าอีเมลติดต่อ** — สร้าง email ที่ใช้ในหน้ากฎหมาย
- [ ] **10.2 Error Monitoring** — พิจารณาใช้ Sentry หรือ Vercel Analytics
- [ ] **10.3 Uptime Monitoring** — ใช้ UptimeRobot หรือ Better Uptime (ฟรี)
- [ ] **10.4 Google Analytics / Vercel Analytics** — ติดตาม traffic

---

## 🔁 Phase 11: ก่อนเปิดใช้งาน (Final Steps)

- [ ] **11.1 Merge `polish110269` → `main`** — หลังตรวจสอบทุกอย่างแล้ว
- [ ] **11.2 Deploy to production**
- [ ] **11.3 Test production URL** — ทดสอบทุก flow บน URL จริง
- [ ] **11.4 ตั้ง Supabase Auth redirect URLs** — ใช้โดเมนจริง
- [ ] **11.5 อัปเดต Legal Pages** — ใส่โดเมนจริง, เบอร์โทรจริง
- [ ] **11.6 Social Media Preview** — ทดสอบ share URL บน LINE, Facebook
- [ ] **11.7 ทดสอบ User Flow ทั้งหมด** — สมัคร → login → รายงาน → ดู dashboard

---

## 🎯 สรุปสิ่งที่ต้องดำเนินการเร่งด่วน (Critical Issues)

| # | ปัญหา | ไฟล์ | ความเร่งด่วน |
|---|--------|------|:---:|
| 1 | `lang="en"` → `"th"` | `layout.tsx` | 🔴 สูง |
| 2 | รูปภาพใหญ่เกินไป (9.9 MB!) | `/public/rice_plant.jpg` | 🔴 สูง |
| 3 | เบอร์โทร Placeholder `0-XXXX-XXXX` | `privacy/page.tsx` | 🔴 สูง |
| 4 | อีเมล Placeholder | `privacy/page.tsx` | 🟡 กลาง |
| 5 | ขาด `robots.txt` & `sitemap.xml` | `/public/` | 🟡 กลาง |
| 6 | ไฟล์ test ที่ root | root directory | 🟢 ต่ำ |
| 7 | ไฟล์ default ที่ไม่ใช้ (SVGs) | `/public/` | 🟢 ต่ำ |
| 8 | `ALLOWED_ORIGIN` เป็น `"*"` | `next.config.ts` | 🔴 สูง (ตอน deploy) |
